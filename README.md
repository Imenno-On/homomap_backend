# Homomap Backend

Backend API для создания карты гомографии помещений с анализом шагов человека и расчетом реальных размеров.

## Описание

Система обрабатывает видео помещения, где человек ходит по комнате. Используя анализ шагов человека (средняя длина шага 75 см), система автоматически калибрует масштаб и рассчитывает реальные размеры помещения в метрах.

## Технологии

- **Python 3.10**
- **FastAPI** - веб-фреймворк
- **PostgreSQL** - база данных
- **SQLAlchemy** - ORM
- **Docker & Docker Compose** - контейнеризация
- **PyTorch** - ML модели для сегментации и глубины
- **YOLOv8** - детекция позы человека
- **OpenCV** - обработка видео

## Архитектура

Проект состоит из двух сервисов:

1. **API сервис** (`app/`) - FastAPI приложение для обработки запросов
2. **ML сервис** (`ml_service/`) - сервис для обработки видео и анализа шагов

## Функциональность

### Основные возможности

- 🔐 **Авторизация** - регистрация и вход пользователей
- 📤 **Загрузка видео** - обработка видеофайлов
- 🧠 **Анализ геометрии** - построение матрицы гомографии на основе первого кадра
- 👣 **Анализ шагов** - трекинг человека и определение шагов для калибровки масштаба
- 📏 **Расчет размеров** - автоматический расчет реальных размеров помещения в метрах
- 💾 **Хранение проектов** - сохранение результатов в БД

### API Эндпоинты

#### Авторизация
- `POST /api/v1/users/register` - Регистрация пользователя
- `POST /api/v1/users/login` - Вход
- `GET /api/v1/users/me` - Информация о текущем пользователе

#### Обработка видео
- `POST /api/v1/homography/process_video` - Обработка видео и анализ шагов

#### Проекты
- `GET /api/v1/projects/` - Список проектов пользователя
- `GET /api/v1/projects/{id}` - Детали проекта
- `DELETE /api/v1/projects/{id}` - Удаление проекта

## Установка и запуск

### Требования

- Docker и Docker Compose
- (Опционально) Python 3.10+ для локальной разработки

### Быстрый старт

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd homomap_backend
```

2. **Создайте файл `.env`:**
```bash
# База данных
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=homomap
DATABASE_URL=postgresql://postgres:postgres@db:5432/homomap

# ML сервис
ML_SERVICE_URL=http://ml:8000

# JWT
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256

# CORS
FRONTEND_ORIGINS=http://localhost:3000,http://localhost:5173
```

3. **Запустите контейнеры:**
```bash
docker-compose up --build -d
```

4. **Проверьте статус:**
```bash
docker-compose ps
```

5. **API документация доступна по адресу:**
   - Swagger UI: http://localhost:8000/docs
   - ReDoc: http://localhost:8000/redoc

### Миграции БД

При первом запуске автоматически выполняется миграция для добавления новых полей в таблицу `projects`. Если нужно выполнить миграцию вручную:

```bash
docker exec -i homomap_db psql -U postgres -d homomap < migrations/add_new_fields.sql
```

## Структура проекта

```
homomap_backend/
├── app/                    # FastAPI приложение
│   ├── api/               # API эндпоинты
│   │   └── endpoints/
│   │       ├── homography.py  # Обработка видео
│   │       ├── projects.py    # Управление проектами
│   │       └── users.py        # Авторизация
│   ├── db/                # Работа с БД
│   │   ├── models.py      # SQLAlchemy модели
│   │   ├── crud.py        # CRUD операции
│   │   └── migrations.py  # Автоматические миграции
│   ├── schemas/           # Pydantic схемы
│   └── main.py            # Точка входа
├── ml_service/            # ML сервис
│   ├── main.py            # FastAPI сервис для ML
│   ├── homography.py      # Классы Homography и StepAnalyzer
│   └── Dockerfile.ml      # Dockerfile для ML сервиса
├── migrations/            # SQL миграции
├── docker-compose.yml     # Конфигурация Docker Compose
├── Dockerfile             # Dockerfile для API сервиса
└── requirements.txt       # Python зависимости
```

## ML Сервис

ML сервис обрабатывает видео в два этапа:

1. **Анализ геометрии** - построение матрицы гомографии на основе первого кадра
2. **Анализ шагов** - трекинг человека на всем видео и определение шагов для калибровки

### Формат ответа ML сервиса

```python
{
    "homography_matrix": [[...], [...], [...]],  # 3x3 матрица
    "scaled_homography_matrix": [[...], [...], [...]] | null,
    "floor_polygons": [[[x, y], ...], ...],
    "wall_polygons": [[[x, y], ...], ...],
    "trajectory_points": [[x, y, distance], ...],
    "step_peaks": [distance1, distance2, ...],
    "scale_info": {
        "scale_factor": 0.775,  # см/пиксель
        "median_peak_distance": 96.7,
        "real_step_length_cm": 75,
        "num_peaks": 12
    } | null,
    "room_dimensions": {
        "width_m": 4.9,
        "length_m": 2.1,
        "area_sq_m": 10.29
    } | null,
    "preview_image": "base64...",
    "processing_time": {
        "homography": 127.39,
        "step_analysis": 15.2,
        "total": 142.59
    }
}
```

## База данных

### Модель Project

```python
- id: int
- title: str
- video_path: str
- preview_path: str
- homography_matrix: JSON
- scaled_homography_matrix: JSON
- floor_polygons: JSON
- wall_polygons: JSON
- trajectory_points: JSON
- step_peaks: JSON
- scale_info: JSON
- room_dimensions: JSON
- processing_time: JSON
- preview_image: TEXT
- status: str
- created_at: datetime
```

## Разработка

### Локальная разработка (без Docker)

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
pip install -r requirements_ml.txt
```

2. **Настройте БД:**
```bash
# Создайте БД PostgreSQL
createdb homomap

# Выполните миграции
psql -U postgres -d homomap < migrations/add_new_fields.sql
```

3. **Запустите API сервис:**
```bash
cd app
uvicorn main:app --reload --port 8000
```

4. **Запустите ML сервис (в другом терминале):**
```bash
cd ml_service
uvicorn main:app --reload --port 8002
```

### Логирование

Логи доступны через Docker:

```bash
# Логи API сервиса
docker-compose logs -f web

# Логи ML сервиса
docker-compose logs -f ml

# Логи БД
docker-compose logs -f db
```

## Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# База данных
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=homomap
DATABASE_URL=postgresql://postgres:postgres@db:5432/homomap

# JWT
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256

# ML сервис
ML_SERVICE_URL=http://ml:8000

# CORS
FRONTEND_ORIGINS=http://localhost:3000,http://localhost:5173
```

## Производительность

- Обработка первого кадра: ~120-130 секунд
- Анализ шагов (300 кадров): ~15-20 секунд
- Общее время обработки: ~140-150 секунд

Для ускорения можно уменьшить `max_frames` в `ml_service/main.py` (строка 114).

## Лицензия

MIT
